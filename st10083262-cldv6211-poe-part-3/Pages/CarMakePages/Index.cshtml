@page
@model st10083262_cldv6211_poe_part_3.Pages.CarMakePages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarMake[0].ManufacturerName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CarMake) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ManufacturerName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MakeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MakeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MakeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
