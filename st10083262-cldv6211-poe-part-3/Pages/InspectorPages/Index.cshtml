@page
@model st10083262_cldv6211_poe_part_3.Pages.InspectorPages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Inspector[0].PersonalDetails)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Inspector) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PersonalDetails.DetailId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.InspectorId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.InspectorId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.InspectorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
