@page
@model st10083262_cldv6211_poe_part_3.Pages.CarBodyTypePages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarBodyType[0].TypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CarBodyType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BodyTypeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BodyTypeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BodyTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
