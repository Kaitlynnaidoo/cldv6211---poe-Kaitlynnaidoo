@page
@model st10083262_cldv6211_poe_part_3.Pages.DriverPages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Driver[0].ResidenceAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Driver[0].PersonalDetails)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Driver) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResidenceAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonalDetails.DetailId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DriverId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DriverId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DriverId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
